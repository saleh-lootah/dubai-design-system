# Review Baseline - 2026-08-20

Measured with Node 22.14.0, Stencil 4.43.5, 34 components. Branch
`chore/codebase-review-harness`. All commands run from `packages/stencil` unless noted.
Every number below was produced by re-running the tool during this task, not copied from
an earlier task's report — see "Reproduction against prior task reports" for where a
number moved and where it held.

## Totals

| Check | Faults |
| --- | --- |
| ESLint errors | 46 |
| ESLint warnings | 4 |
| Prettier unformatted files (`format:check`) | 93 |
| Ghost attributes (`check:api`) | 106 (1 of these, `dda-breadcrumb`'s `breadcrumbs`, is informational — see below) |
| axe violation instances, light theme | 16 |
| axe violation instances, dark theme | 29 |
| axe distinct failing stories, light | 16 |
| axe distinct failing stories, dark | 25 |
| axe distinct failing stories, union (light ∪ dark) | 29 |
| Target size (WCAG 2.5.8) | 4 |
| Keyboard reach (WCAG 2.1.1) | 0 |
| WCAG 2.4.7 focus-visible — confirmed failures | 13 |
| WCAG 2.4.7 focus-visible — needs-manual-contrast-check (not a confirmed failure) | 15 |
| Unit and e2e tests failing | 0 (41/41 pass, 6 suites) |

**Read the axe rows and the 2.4.7 rows together with the caveats below before treating
either as a stable count.**

### Caveat: the axe numbers are provisional, not a stable invariant

The full sweep (`npm run test:a11y:ci`, then a bare `test-storybook --json` rerun against
the same `storybook-static` build for per-story detail) was run twice in this task. Both
runs were byte-identical: same 35 failing tests, same messages, same
`Components/Header Default` dark-theme result (2 violations: `color-contrast` +
`link-name`). **That agreement is not independent confirmation.** Both runs reused the
same unrebuilt `storybook-static` output and the same warm Storybook/Jest cache in the
same process lifetime — they are correlated samples of one build, not two separate
measurements. `Components/Header Default` (dark theme) has an unstable history across
this whole review effort: Task 3's original run caught 1 violation once and passed clean
twice on isolated reruns; Task 3's fix round caught 2 violations on both of its runs;
this task's two runs also both caught 2 violations. The story has flipped among
0/1/2 violations across at least five independent measurement sessions now. **Treat the
dark-theme axe count (29 instances / 25 distinct stories) as reproducible only in the
narrow sense of "reproduced twice against the same build," not as a number guaranteed to
recur against a fresh `storybook-static` rebuild or on different hardware.** The light
side (16/16) has never varied across any task's measurement.

### WCAG 2.4.7 split — do not merge these two rows

**13 confirmed failures** (`dda-button`, `dda-creditcard-field`, `dda-dropdown`,
`dda-input`, `dda-link-button`, `dda-search-input`, `dda-select`, `dda-textarea`) are
cases where Tab-focusing the element produces *no visible change at all* — no outline,
box-shadow, or colour change anywhere in the ancestor chain (verified against an
unfocused-state baseline captured before any Tab press). These are real WCAG 2.4.7
defects.

**15 needs-manual-contrast-check** entries (`dda-button`, `dda-link-button`,
`dda-footer`, `dda-home-banner`, `dda-number-field`, `dda-phonefield`,
`dda-search-input`) are cases where focus *does* change something (typically a
background-color, border-color, or text-color) but no `outline` or `box-shadow` appears
anywhere in the ancestor chain. WCAG 2.4.7 permits a focus indicator through means other
than outline/box-shadow, provided the change carries sufficient contrast against the
unfocused state — a judgment this tool cannot make (it stopped at "cannot find outline
or box-shadow," not "cannot find a discernible change"). These 15 are not confirmed
defects; treat each as needing a human contrast check before deciding whether it's a
fault. Full list, from this task's `console.warn` capture: `components-button--
default-primary/error-primary/surface-primary/icon-button`, `components-link-button--
default-primary/error-primary/surface-primary/icon-button`, `components-footer--default`,
`components-home-banner--default/autoplay`, `components-number-field--disabled/error`,
`components-phone-field--error`, `components-search-input--with-button`.

### Ghost attribute marked informational, not a real defect

`dda-breadcrumb`'s `breadcrumbs` finding (1 of the 106, in `dda-breadcrumb.stories.tsx`)
is a known-benign false-positive-by-design: the story destructures `breadcrumbs` out of
`args` and re-encodes it into a real `data-breadcrumbs` attribute
(`const { breadcrumbs, ...otherArgs } = args;`). The rendered markup works correctly.
It is reported because the checker's contract is "an `argTypes`/`args` key that is not a
declared prop is a finding," and detecting "this key gets destructured out and
re-encoded under a different, real attribute name" needs semantic analysis beyond what
the tool does. **Marked informational, not removed from the count** — the total above is
still 106.

### Coverage gap: 2 of 34 components have no Storybook stories

`dda-banner` and `dda-ui-card` have no `.stories.tsx` file. They were linted and
API-checked (both show 0 ghost attributes), but the axe sweep, WCAG 2.5.8, and WCAG 2.1.1
checks only run against stories — these two components were never exercised by any of
those three tools. Their `0` cells below mean "not measured," not "measured and clean."

## Faults by component

axe and 2.4.7 columns count **distinct stories** (a story with 2 violations in one theme
still counts once). ESLint is `errors/warnings`. 2.5.8 and 2.1.1 count failing stories.

| Component | ESLint | Ghost attrs | axe (union, light∪dark) | 2.5.8 | 2.1.1 (Keyboard) | 2.4.7 confirmed | 2.4.7 needs-manual |
| --- | --- | --- | --- | --- | --- | --- | --- |
| dda-accordion | 3/0 | 0 | 0 | 0 | 0 | 0 | 0 |
| dda-alert | 1/0 | 0 | 2 | 0 | 0 | 0 | 0 |
| dda-attach-file | 1/0 | 0 | 4 | 0 | 0 | 0 | 0 |
| dda-avatar | 3/0 | 4 | 1 | 0 | 0 | 0 | 0 |
| dda-banner | 1/0 | 0 | 0* | 0* | 0* | 0* | 0* |
| dda-breadcrumb | 1/0 | 1† | 0 | 0 | 0 | 0 | 0 |
| dda-button | 1/0 | 6 | 1 | 0 | 0 | 1 | 4 |
| dda-checkbox | 1/0 | 1 | 0 | 0 | 0 | 0 | 0 |
| dda-chip | 3/0 | 4 | 1 | 0 | 0 | 0 | 0 |
| dda-credit-card | 1/0 | 0 | 2 | 0 | 0 | 0 | 0 |
| dda-creditcard-field | 1/0 | 0 | 1 | 0 | 0 | 1 | 0 |
| dda-dropdown | 1/0 | 1 | 0 | 0 | 0 | 1 | 0 |
| dda-footer | 1/0 | 0 | 0 | 0 | 0 | 0 | 1 |
| dda-header | 6/4 | 18 | 1 | 0 | 0 | 0 | 0 |
| dda-home-banner | 1/0 | 0 | 0 | 0 | 0 | 0 | 2 |
| dda-horizontal-stepper | 1/0 | 2 | 1 | 0 | 0 | 0 | 0 |
| dda-input | 1/0 | 0 | 1 | 0 | 0 | 2 | 0 |
| dda-link-button | 1/0 | 3 | 1 | 0 | 0 | 1 | 4 |
| dda-number-field | 1/0 | 0 | 1 | 0 | 0 | 0 | 2 |
| dda-pagination | 1/0 | 0 | 1 | 1 | 0 | 0 | 0 |
| dda-phonefield | 1/0 | 2 | 1 | 0 | 0 | 0 | 1 |
| dda-progressbar | 1/0 | 0 | 1 | 0 | 0 | 0 | 0 |
| dda-radiobutton | 1/0 | 1 | 0 | 0 | 0 | 0 | 0 |
| dda-range-slider | 1/0 | 0 | 3 | 3 | 0 | 0 | 0 |
| dda-search-input | 1/0 | 8 | 1 | 0 | 0 | 2 | 1 |
| dda-segmented-tabs | 1/0 | 0 | 0 | 0 | 0 | 0 | 0 |
| dda-select | 1/0 | 3 | 1 | 0 | 0 | 2 | 0 |
| dda-sticky-footer | 1/0 | 43 | 0 | 0 | 0 | 0 | 0 |
| dda-tabs | 1/0 | 0 | 2 | 0 | 0 | 0 | 0 |
| dda-textarea | 1/0 | 0 | 2 | 0 | 0 | 3 | 0 |
| dda-toggle | 1/0 | 5 | 0 | 0 | 0 | 0 | 0 |
| dda-tooltip | 1/0 | 0 | 0 | 0 | 0 | 0 | 0 |
| dda-ui-card | 1/0 | 0 | 0* | 0* | 0* | 0* | 0* |
| dda-vertical-stepper | 1/0 | 4 | 0 | 0 | 0 | 0 | 0 |
| **Totals** | **45/4 + 1 error in `src/utils/applyTheme.js`, not attributable to a component (see below) = 46/4** | **106** | **29** | **4** | **0** | **13** | **15** |

\* `dda-banner` and `dda-ui-card` have no Storybook stories — not measured by axe/2.5.8/2.1.1/2.4.7, see coverage gap note above.

† `dda-breadcrumb`'s ghost attribute is the known-benign informational finding, see above — not a real defect, still counted in the total per the checker's literal contract.

`src/utils/applyTheme.js` carries 1 ESLint error (`no-undef` on `document`) that is not
attributable to any single component — see "Repo-level defects" below for why.

## Reproduction against prior task reports

| Tool | Prior report(s) | This run | Reproduced? |
| --- | --- | --- | --- |
| ESLint | 46 errors, 4 warnings (Task 1) | 46 errors, 4 warnings | Yes, exactly |
| Prettier | 93 unformatted files (Task 1) | 93 unformatted files | Yes, exactly |
| check:api | 106 ghost attributes (Task 2, fix round) | 106 | Yes, exactly |
| Component tests | 41/41 pass, 6 suites (implied stable) | 41/41 pass, 6 suites | Yes, exactly |
| axe light | 16 instances / 16 stories (Task 3 fix round) | 16 / 16 | Yes, exactly |
| axe dark | 29 instances / 25 stories (Task 3 fix round) | 29 / 25 | Yes, matches Task 3's fix-round figure — **not** Task 3's original-report figure (27/24) or Task 4's rerun figure (28 instances), both attributable to the same `dda-header` flake (see caveat above) |
| axe union | 29 distinct stories (Task 3/4) | 29 | Yes, exactly |
| WCAG 2.5.8 | 4 failures, `dda-pagination` + `dda-range-slider` (Task 4 fix round) | 4, same 2 components, same stories | Yes, exactly |
| WCAG 2.1.1 | 0 failures (Task 4 fix round, down from 21 pre-fix) | 0 | Yes, exactly |
| WCAG 2.4.7 confirmed | 13 (Task 4 fix round) | 13, same 8 components | Yes, exactly |
| WCAG 2.4.7 needs-manual | 15 (Task 4 fix round) | 15, same 7 components | Yes, exactly |

**Everything reproduced.** The only figure that did not match a prior report's headline
number verbatim is axe-dark, and that is explained fully by the already-documented
`dda-header` flake — this run's 29/25 matches Task 3's own *fix-round* corrected number,
not its original pre-fix number, and not Task 4's independent rerun (which caught the
flake in its low state). No new discrepancy was introduced by this task.

## Also recorded: findings the counters above don't cover

Carried forward from Tasks 1-4 for Task 7/9, confirmed still present on this branch as of
this run:

- **`dda-footer` and `dda-sticky-footer` render with an open shadow root**, against
  CLAUDE.md's stated `shadow: false` + global-CSS convention. Confirmed by Task 4's
  direct DOM instrumentation (naive `document.activeElement` only ever resolved to the
  `<dda-footer>` host, not the real focused descendant, until the check was rewritten to
  drill through `shadowRoot.activeElement`).
- **`dda-tabs.tsx:59` applies the same `id` to every generated tab button** — a
  duplicate-ID bug surfaced as a side effect of Task 4's keyboard-reach identity fix
  (`components-tabs--text-tabs` was undercounted 2/3 reachable until the check stopped
  conflating siblings that shared this `id`).
- **`dda-number-field`'s "Disabled" story never actually disables its input** —
  `dda-number-field` has no `disabled` `@Prop()` at all, so the story's disabled-looking
  state is styling only, not a real `disabled` attribute on the `<input>`.
- **`packages/angular/projects/stencil-wrapper/package.json`** was invalid JSON (trailing
  comma) from the initial commit until fixed on this branch. Confirmed valid now
  (`node -e "JSON.parse(...)"` succeeds).
- **`package-lock.json`** declared only the Windows native binary for `@swc/core`, so no
  Linux machine could build Storybook, until fixed on this branch. Confirmed now: the
  lock declares all 10 `@swc/core-*` platform packages (darwin-arm64, darwin-x64,
  linux-arm64-gnu, linux-arm64-musl, linux-arm-gnueabihf, linux-x64-gnu, linux-x64-musl,
  win32-arm64-msvc, win32-ia32-msvc, win32-x64-msvc).
- **`@stencil/core` version drift** — the lock file pinned 4.24.0 while the working tree
  had drifted to 4.43.5, so `npm ci` produced a different build from what developers
  test. Now pinned exactly: `packages/stencil/package.json` and `package-lock.json` both
  declare `4.43.5`. Confirmed by this task's own `npx stencil build` output
  (`v4.43.5`).
- **ESLint's `files: ['src/**/*.tsx']` scope excludes `.js` files.**
  `src/utils/applyTheme.js` is outside that glob, so its `no-undef: off` override (added
  for `.tsx` files, where Stencil's JSX globals would otherwise false-positive) never
  reaches it, and the file escapes linting entirely except for whatever the base config
  still applies. This is why its 1 `no-undef` error (on `document`) shows up in the
  Totals table but has no home in the per-component table — it isn't a component file.

## Raw output

The raw files are in this folder: `raw-build.txt`, `raw-lint.txt`, `raw-format.txt`,
`raw-api.txt`, `raw-tests.txt`, `raw-a11y.txt`. `eslint-report.json` (JSON form of the
lint results, written by `npm run lint:report`) is committed alongside this document.

`raw-a11y.txt` is the single `npm run test:a11y:ci` run specified by the brief (Test
Suites: 21 failed, 11 passed, 32 total; Tests: 35 failed, 71 passed, 106 total). The
per-story/per-component breakdown above (axe light/dark split, 2.5.8, 2.1.1, 2.4.7
confirmed/needs-manual) required a second, `--json`-output variation of the same command
against the same `storybook-static` build to get machine-parseable detail; that variation
was run twice to check stability (see the axe caveat above) but its JSON output was not
committed, since it is a derived/regenerable run artifact, not a distinct tool result —
`raw-a11y.txt` already carries the tool's real, committed output.
