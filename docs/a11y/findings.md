# Code Review Findings

Synthesis of `docs/a11y/baseline-2026-08-20.md` (automated tools) and
`docs/a11y/manual-review.md` (hand review) — both dated 2026-08-20, branch
`chore/codebase-review-harness`. This document adds no new investigation; every row
below traces to one or both source documents. Read those two first for full method and
raw evidence (`docs/a11y/raw-*.txt`).

Severity: **CRITICAL** breaks a user task or a component's core functionality entirely
(a total lockout or total non-function, not a partial degradation). **HIGH** fails WCAG
at level A or AA with the component still partially usable. **MEDIUM** is a fault a user
can work around, or a defect without a clean WCAG citation. **LOW** is a defect in
quality (lint, formatting, doc completeness, code smell) with no functional or
accessibility consequence.

The table is sorted CRITICAL → LOW. Three categories sit outside that ranking, in their
own sections at the end, because forcing them into CRITICAL–LOW would overstate or
understate what is actually known: **Open Items** (genuinely unresolved — need a live
browser/AT check, not a pass or fail from source alone) and **Informational** (confirmed
non-defects, kept for traceability, not queued for repair).

## A resolved disagreement between the two source documents, called out explicitly

The baseline's automated WCAG 2.1.1 (keyboard reachability) sweep reports **0**
failures project-wide. The manual review found **3 real, confirmed keyboard lockouts**
the automated number misses entirely: `dda-accordion` (F-001), `dda-avatar` (F-002),
`dda-chip` (F-003). This is not the automated tool being wrong about what it measured —
it is a structural blind spot, explained and cited at F-046: the checker's `INTERACTIVE`
selector only verifies that elements *already marked* interactive are reachable; a
`<div>`/`<span>` with an `onClick` and no `tabindex` was never a candidate in the first
place, so failing to reach it can't register as a shortfall. Both numbers are recorded
here — 0 from the tool, 3 confirmed by source reading — rather than one silently
overriding the other.

---

## Findings

| ID | Component | Severity | Fault | WCAG | Repair |
| --- | --- | --- | --- | --- | --- |
| F-001 | dda-accordion | CRITICAL | The header is a plain `div` with `onClick`, no `tabindex`, `role`, or `onKeyDown` — no nested focusable element exists either. A keyboard-only user cannot reach or open the accordion at all. Not caught by the automated 2.1.1 sweep (see F-046). `dda-accordion.tsx:26,30-34` | 2.1.1 | Make the header a real `button` (or add `tabindex="0"`, `role="button"`, and an `onKeyDown` for Enter/Space) and add `aria-expanded`. |
| F-002 | dda-avatar | CRITICAL | The entire dropdown-toggle root element is a `div` with `onClick`, no `tabindex`/`role`/`onKeyDown`. A keyboard user cannot open the avatar dropdown at all. `dda-avatar.tsx:45` | 2.1.1 | Change the root trigger to a `button`, or add `tabindex="0"` + `role="button"` + `onKeyDown`. |
| F-003 | dda-chip | CRITICAL | The close ("dismiss") control is a `span` with `onClick`, no `tabindex`/`role`/`onKeyDown`, and it is the chip's only dismiss mechanism when `show_close_icon` is set. A keyboard user cannot dismiss a chip. `dda-chip.tsx:25` | 2.1.1 | Change `.chip-close` to a real `button`. |
| F-004 | dda-segmented-tabs | CRITICAL | No `onClick` handler, no `@State`, no selection tracking anywhere in the file. Clicking a segment does nothing — not a keyboard-vs-mouse gap, the component has no interactivity at all. `dda-segmented-tabs.tsx:21-31` | — (functional, prerequisite to any accessibility fix) | Wire a click handler and selected-state tracking before adding ARIA (`role="tablist"`/`"tab"`, `aria-selected`). |
| F-005 | dda-footer | CRITICAL | `shadow: true` (`dda-footer.tsx:6`), against CLAUDE.md's stated `shadow: false` convention, and its own `dda-footer.css` is a confirmed 0-byte file. No CSS — local or global — can reach inside the shadow root. The component renders as unstyled raw HTML; every utility class its markup depends on (`dda-container`, `dda-flex`, `dda-row`, etc.) is defined only in the unreachable `global/global.css`. | 1.4.11 / 2.4.7 (cascading; no authored CSS exists to test against) | Either drop `shadow: true` to match the library convention, or give `dda-footer.css` real content (own rules, or `@import`/bundle `global.css` the way `dda-banner` does — see I-002-adjacent pattern at `dda-banner.tsx:5`). |
| F-006 | dda-sticky-footer | CRITICAL | Same root cause as F-005 (`shadow: true` at `dda-sticky-footer.tsx:7`, `dda-sticky-footer.css` confirmed 0 bytes, the `styleUrls` line that would import `global.css` is commented out at `:6`) — but here it additionally means the component's *designed* behavior never activates: `.dda-footer{position:fixed;...}` lives only in `global/templates/dda-footer-main.css`, unreachable from the shadow root, so the footer isn't fixed to the viewport at all; and the `hidden` class toggled by its own scroll-direction state (`dda-sticky-footer.tsx:65-66,82-85,91`) has no CSS defining it anywhere, so the show/hide-on-scroll behavior has no visible effect either. As shipped this is an inert, unstyled block-flow `<footer>`, not a sticky footer. | 1.4.11 (cascading; component does not implement its designed behavior at all) | Same repair path as F-005, applied to `dda-sticky-footer.css`; must add the `.dda-footer{position:fixed;...}` and `.hidden` rules (or reach `global.css`), not just any CSS. |
| F-007 | (repo-level) | CRITICAL — **fixed on this branch** | `packages/angular/projects/stencil-wrapper/package.json` was invalid JSON (a trailing comma) from the initial commit onward, breaking that package's build entirely. | — | Fixed on this branch; confirmed valid now (`node -e "JSON.parse(...)"` succeeds). No further action. |
| F-008 | (repo-level) | CRITICAL — **fixed on this branch** | `package-lock.json` declared only the Windows native binary for `@swc/core`, so no Linux machine could build Storybook at all. | — | Fixed on this branch; lock now declares all 10 `@swc/core-*` platform packages. No further action. |
| F-009 | dda-header | HIGH | Fixed to the viewport (`position: fixed; top: 0; z-index: 6`, `global/templates/dda-header-main.css:1-10`, reached via `shadow: false`) with zero `scroll-padding`/`scroll-margin` anywhere in the codebase (grepped the full `global/` tree — no matches). A keyboard user tabbing to an element near the top of a scrolled page risks the browser landing that element directly under the fixed header. Absence of the mitigation is a confirmed code fact; the exact on-screen pixel overlap on any given page is not (see O-007). | 2.4.11 | Add `scroll-padding-top` (or per-element `scroll-margin-top`) sized to the header's rendered height. |
| F-010 | dda-tabs | HIGH | No WAI tabs pattern implemented on any axis: no `role="tablist"`/`"tab"` (`dda-tabs.tsx:56-68`, plain `div`/`button`), `aria-selected` reflects only a CSS class not an ARIA state, no `aria-controls`, and no panel is rendered by the component at all. No arrow-key handling exists (`onKeyDown`/`ArrowLeft`/`ArrowRight` grepped, zero matches) — every tab is a plain tabbable `button`, so `Tab` moves through every tab sequentially instead of a roving `tabindex`, the opposite of the pattern's intent. Compounding this, `<button id={this.button_id}>` sits inside `.map()` (`dda-tabs.tsx:59`), so every tab in an instance with more than one tab shares one literal `id` — see F-025. No `.e2e.ts`/`.spec.tsx` exists for this component. | 4.1.2 | Add `role="tablist"`/`"tab"`, real `aria-selected`/`aria-controls`, arrow-key roving-tabindex handling, and give each generated button a unique `id` (see F-025's repair). |
| F-011 | dda-tooltip | HIGH | Pure-CSS, `:hover`-only trigger (`dda-tooltip.css:17-20`); no JavaScript, no `:focus`/`:focus-within` counterpart, no `Escape` handling. A keyboard user who tabs to the trigger never sees the tooltip appear at all — content is entirely inaccessible by keyboard, a more basic gap than any single WCAG 1.4.13 sub-criterion. Of the three 1.4.13 sub-criteria specifically: hoverable (pass, the box is a DOM descendant of the hover container), persistent/no auto-hide (pass, no `setTimeout` in the file), dismissible without moving the pointer (fail — no `Escape` handler exists). | 1.4.13 (dismissible sub-criterion fails outright; keyboard-parity gap is broader than 1.4.13 alone) | Add a `:focus-within` trigger alongside `:hover`, and an `Escape` handler to dismiss without moving the pointer. |
| F-012 | dda-alert | HIGH | No `role` attribute anywhere in the file (grepped, zero matches). A screen reader user is never notified when a `dda-alert` is inserted into the page. `dda-alert.tsx:36` | 4.1.3 | Add `role="alert"` (or `role="status"` for less urgent variants) to the root element. |
| F-013 | dda-progressbar | HIGH | No `role`, `aria-valuenow`, `aria-valuemin`, or `aria-valuemax` anywhere (grepped, zero matches on all four). The bar conveys `this.progress` only through inline `width`. `dda-progressbar.tsx:16-39` | 4.1.2 | Add `role="progressbar"` plus `aria-valuenow`/`aria-valuemin`/`aria-valuemax` bound to `this.progress`. |
| F-014 | dda-select | HIGH | Custom combobox-like widget with no `aria-haspopup`, `aria-expanded`, or `aria-controls` on the trigger button, and no `role="listbox"`/`"option"`/`aria-selected` on the option list. A screen reader user gets no indication the button opens a list, whether it's open, or which option is selected. `dda-select.tsx:56-69` | 4.1.2 | Implement the ARIA listbox/combobox pattern: `aria-haspopup="listbox"`, `aria-expanded`, `aria-controls` on the trigger; `role="listbox"` on the list, `role="option"` + `aria-selected` on each item. |
| F-015 | dda-textarea | HIGH | When `enable_rich_editor` is true, `render()`'s ternary emits a Quill `<div id="editor">` instead of the labelled `<textarea>` the `<label for>` targets — the label association that works in plain mode is silently absent in rich-editor mode. `dda-textarea.tsx:86-100` | 1.3.1 / 4.1.2 | Give the Quill container the same `id`/label wiring, or an equivalent `aria-labelledby`. |
| F-016 | dda-input, dda-textarea, dda-select, dda-number-field, dda-search-input, dda-phonefield, dda-creditcard-field, dda-attach-file | HIGH | Systemic, confirmed across all 8 components that have an `error_message` prop: the error text renders as a plain sibling `<span>`/`<div>` with no `id`, and the `<input>`/`<textarea>` carries no `aria-describedby` pointing at it and no `aria-invalid`. A screen reader user tabbing into a field in an error state hears the label only, never the error text. Representative: `dda-input.tsx:45-54,56`. Grep confirming zero matches for `aria-describedby`/`aria-invalid`/`autocomplete` across all 11 form components: `dda-input`, `dda-textarea`, `dda-select`, `dda-checkbox`, `dda-radiobutton`, `dda-toggle`, `dda-number-field`, `dda-search-input`, `dda-phonefield`, `dda-creditcard-field`, `dda-attach-file`. | 1.3.1 / 4.1.2 (also related to 3.3.1) | Give each error-message element a stable `id` and set `aria-describedby={id}` (and `aria-invalid="true"` when `error_message` is present) on the associated input. |
| F-017 | dda-search-input | HIGH | The visible `<label>` targets `this.button_id` (`dda-search-input.tsx:39`), but `button_id` is applied to the **clear/close button** (`:44`), not the text field. The actual search `<input>` (`:42`) has a hardcoded `id='search'` that nothing points at. When a consumer sets `label`, a screen reader announces it as the clear button's name, not the search field's — a real mis-association, worse than a missing one. | 1.3.1 / 4.1.2 | Point the `<label for>` at the search input's own id (not `button_id`), and give the search `<input>` a prop-driven, non-hardcoded `id` (see F-025 for the related duplicate-id issue on the same element). |
| F-018 | dda-creditcard-field, dda-phonefield | HIGH | No `autocomplete` prop exists on either component (`dda-creditcard-field.tsx:9-24`, `dda-phonefield.tsx:10-31`) — exactly the two field types (card number, phone) WCAG 1.3.5 names as requiring `autocomplete="cc-number"`/`"tel"`. | 1.3.5 / 3.3.7 | Add an `autocomplete` `@Prop()` (or hardcode the correct token) on both components' primary `<input>`. |
| F-019 | dda-button, dda-link-button, dda-home-banner, dda-search-input | HIGH | 9 of the 15 baseline "needs-manual-contrast-check" entries: `.btn-color-default-primary:focus`, `.btn-color-error-primary:focus`, and their `IconButton`-mapped equivalents restore focus indication with a malformed single-value shorthand, e.g. `outline: var(--dda-on-primary-variant-20);` (`dda-button.css:25-28` and equivalents at `:58-61,72-74,130-134,145-149,162-165,176-179`, plus the reachable `.light-mode` variants at `:333-336,347-351,363-367,378-382,395-399,411-415,427-431,442-446,458-462,473-477` — confirmed live code, see I-002). `outline: <color>` is syntactically legal but resets `outline-style` to its initial value, `none` — so no outline ever renders, regardless of color. Computed WCAG relative-luminance contrast between resting and focused background, using the real tokens (`global/color.css:1-76,150-210`): `default-primary` 2.04:1 (light) / 2.08:1 (dark); `error-primary` 1.45:1 (light) / 1.77:1 (dark) — both themes fail the 3:1 threshold on all three affected variants. Affects, via composition: `dda-home-banner--default`/`--autoplay` (slotted `<dda-button>`, `dda-home-banner.stories.tsx:12`) and `dda-search-input--with-button` (`dda-search-input.tsx:46`). `dda-link-button` shares the same classes via `styleUrls: ['../../global/dda-button.css', ...]` (`dda-link-button.tsx:5`). | 2.4.7 | Fix the shorthand: use `outline-color: var(...)` plus an explicit `outline-style`/`outline-width` (or `outline: 2px solid var(...)`), not a bare color value. |
| F-020 | dda-button, dda-link-button | HIGH | 2 more of the 15 baseline needs-manual-contrast entries (together with F-019's 9, this makes 11 of 15 confirmed as WCAG 2.4.7 failures — the other 4 of 15 are not part of this count: 3 are open items at O-001, and 1 (`dda-footer`) is folded into F-005 as unmeasurable rather than a contrast verdict): `.btn-color-onsurface-primary:focus` (`dda-button.css:234-236`, and its `.light-mode` counterpart `:248-251`) declares **no `outline` property at all** — it never attempts the malformed shorthand, it simply never overrides `.dda-btn{outline:0}` (`dda-button.css:8`), so the base rule governs by cascade. Computed contrast: 2.24:1 (light) / 1.70:1 (dark) — both fail 3:1. This is a distinct mechanism from F-019 (no declaration vs. a broken one) reaching the same root cause. | 2.4.7 | Add an explicit `outline` (or `box-shadow`) declaration to `.btn-color-onsurface-primary:focus` and its `.light-mode` counterpart. |
| F-021 | dda-button, dda-creditcard-field, dda-dropdown, dda-input, dda-link-button, dda-search-input, dda-select, dda-textarea | HIGH | 13 confirmed WCAG 2.4.7 failures where Tab-focusing produces no visible change at all — no outline, box-shadow, or color change anywhere in the ancestor chain (verified against an unfocused-state baseline). | 2.4.7 | Add a visible focus indicator (outline or box-shadow) to each; root cause for the button/link-button share is the same `.dda-btn{outline:0}` as F-019/F-020. |
| F-022 | dda-pagination, dda-range-slider | HIGH | 4 failing stories for target size (interactive elements below the minimum touch-target size). | 2.5.8 | Increase hit area (padding or `min-width`/`min-height`) on the affected controls; see `raw-a11y.txt` for the exact failing stories. |
| F-023 | (multiple — 29 distinct stories) | HIGH | 29 distinct Storybook stories fail axe-core checks in at least one theme (union of light ∪ dark), stable across every run and every task to date. Per-story axe rule IDs (which map to specific WCAG SCs) are not broken out per-component in the baseline beyond `dda-header`'s known `color-contrast` + `link-name` pair; the rest require reading `raw-a11y.txt`/`raw-a11y-rerun{1,2}.txt` directly. See F-037 for why the light-theme count specifically (14 vs 16) is not yet a stable number. | Varies by axe rule — see raw files | Triage per-story axe output in `raw-a11y.txt` (`npm run test:a11y:ci` from `packages/stencil`) and fix per component. |
| F-024 | dda-dropdown | MEDIUM | No `Escape` handler, no outside-click handler, no `.focus()` call anywhere in the file (grepped for `Escape`/`keydown`/`.focus(`/`outside`/`blur` — zero matches on every term). The only ways to close it are picking an option or re-clicking the trigger; focus is never programmatically returned to the trigger. A workaround exists (tab back to the same button and reactivate), so this doesn't rise to a lockout. `dda-dropdown.tsx` (whole file) | — (WAI-ARIA menu/listbox authoring-practice deviation, not a clean numbered SC) | Add an `Escape` key handler that closes the dropdown and returns focus to the trigger button. |
| F-025 | dda-tabs, dda-search-input, dda-phonefield, dda-avatar | MEDIUM | The same bug recurs 4 independent times: a single static `@Prop() button_id`/`id` is applied inside a `.map()`, so every generated element in one component instance shares one literal `id`. `dda-tabs.tsx:59` (every tab), `dda-search-input.tsx:42` (hardcoded `id='search'`, collides across page instances), `dda-phonefield.tsx:82` (every one of 200+ country options), `dda-avatar.tsx:59` (every dropdown option). Strongly suggests a copy-paste-propagated pattern rather than 4 unrelated mistakes. | 4.1.1 (historical; removed as a distinct SC in WCAG 2.2 but still a real DOM-validity/AT defect) | Derive each generated element's `id` from the prop plus the loop index (e.g. `${this.button_id}-${index}`) in all 4 components — one systemic fix, not four. |
| F-026 | dda-banner | MEDIUM | `dda-banner-slider`/`dda-banner-slide` (`dda-banner.tsx:21,23`) have no CSS backing anywhere — grepped `dda-banner.css` (28 bytes, just `:host{display:block}`) and `global/global.css`, neither defines them — so the "slider" has no layout styling (no flex/grid/overflow/scroll-snap), just stacked images with inline `width`/`height`. There is also no autoplay, navigation, or interactivity of any kind (`@State` beyond the parsed slide array: none). `dda-banner` does correctly import `global.css` into its shadow bundle (`dda-banner.tsx:5`, unlike F-005/F-006's components) — the layout gap is independent of the shadow-DOM issue. | — (functional/design defect, not a numbered SC; content still displays, degraded rather than broken) | Give the slider classes real layout CSS and decide/implement an interaction model (static grid vs. actual carousel) before writing its story (see F-039). |
| F-027 | dda-breadcrumb, dda-pagination, dda-horizontal-stepper, dda-vertical-stepper | MEDIUM | All four mark the "current" item with a CSS class only, never `aria-current`. `dda-breadcrumb.tsx:31` (`active` class on last crumb), `dda-pagination.tsx:111` (`active` class on current page button), `dda-horizontal-stepper.tsx:24`, `dda-vertical-stepper.tsx:24` (both `active` class on current step). A screen reader user gets no non-visual "you are here" signal in any of the four. | 4.1.2 | Add `aria-current="page"` (breadcrumb/pagination) or `aria-current="step"` (steppers) to the current item. |
| F-028 | dda-toggle | MEDIUM | The visible label text is hardcoded, not driven by any prop: `dda-toggle.tsx:33-34` renders literal `"Radio Button Title"`/`"Supporting Text"` on every instance regardless of what's being toggled — copy-paste residue from `dda-radiobutton`, confirmed by dead, commented-out `@Prop() labelOn`/`labelOff` at `:10-11` that were never reconnected. Every real-world use of `dda-toggle` shows the wrong label text unless the consumer overrides it entirely via `aria_label`. | — (no clean numbered SC — content-accuracy defect with accessibility consequence; loosely 1.3.1) | Add real `label`/`supporting_text` (or similarly named) props and render them in place of the hardcoded strings. |
| F-029 | dda-checkbox | MEDIUM | `dda-checkbox-docs.mdx:28,58,79` documents `on_checked="() => void 0"` as an attribute. No `on_checked` prop or `@Event()` of that name exists (`dda-checkbox.tsx:9-19`). Not caught by `check:api` because that tool scans `.stories.tsx` `argTypes`/`args`, not prose code fences inside `.mdx`. | — (documentation-accuracy defect) | Remove the `on_checked` example from the doc, or document the real event name if one is added. |
| F-030 | dda-toggle | MEDIUM | `dda-toggle-docs.mdx:22-23,51-52,71-72` documents `label_on="On"` / `label_off="Off"` as attributes. Neither exists on the component (only the dead, differently-named `labelOn`/`labelOff` at `dda-toggle.tsx:10-11` — see F-028). Same `check:api` blind spot as F-029 (`.mdx` code fences aren't scanned). | — (documentation-accuracy defect) | Remove the ghost attributes from the doc, or implement the props they describe (see F-028's repair). |
| F-031 | dda-number-field | MEDIUM | The "Disabled" story never actually disables the input because `dda-number-field` has no `disabled` `@Prop()` at all (`dda-number-field.tsx:9-26`) — the story's disabled-looking state is styling only. | — (missing prop, not a WCAG SC on its own) | Add a `disabled` `@Prop()` and apply it to the rendered `<input>`. |
| F-032 | dda-phonefield | MEDIUM | The country-code dropdown trigger button and its option buttons have no label association of their own (`dda-phonefield.tsx:76-78,82`) — they rely on visible text/a flag `alt=""` (both empty, `:77,83`), so a screen reader user hears only the numeric code (e.g. "+971") with no indication it's a "change country" control. | 4.1.2 / 1.1.1 | Add `aria-label` (e.g. "Change country code") to the trigger button; give the flag a meaningful `alt` or mark it `aria-hidden="true"` deliberately if the code text already conveys the country. |
| F-033 | dda-credit-card | MEDIUM | `dda-credit-card.tsx:37-38` (`{this.name}`, `**** {this.card_number.slice(-4)}`) will throw if `card_number` is ever left unset — `undefined.slice` is an unguarded runtime error, not a graceful degradation. | — (robustness defect) | Guard with `this.card_number?.slice(-4)` or a default value. |
| F-034 | dda-vertical-stepper | MEDIUM | `@Prop() current_Step: number = 0;` (`dda-vertical-stepper.tsx:10`) breaks the codebase's stated snake_case prop convention (CLAUDE.md) — every other prop in this file and across the codebase is fully lower-case. Whether the compiled HTML attribute a consumer must set matches the documented name was not verified without a build. | — (API-consumption risk, not verified) | Rename to `current_step` for consistency, and confirm the compiled attribute name matches the docs (see O-006). |
| F-035 | dda-ui-card | MEDIUM | `@Event() linkClick?: EventEmitter<void>;` (`dda-ui-card.tsx:17`) is declared but never emitted anywhere in the file — the `<a>` at `:38-47` that would logically fire it has no `onClick` handler. | — (dead API surface) | Either wire `onClick` on the `<a>` to emit `linkClick`, or remove the unused event before a story documents it as working (see F-039). |
| F-036 | dda-horizontal-stepper | MEDIUM | `@Prop() current_step: number = 1` (`dda-horizontal-stepper.tsx:10`) defaults to `1`, but the active-step comparison is `index === this.current_step` against a 0-based `.map((step, index) => ...)` (`:24`) — so with the documented default, the **second** step (`index === 1`) is marked active out of the box, not the first. | — (off-by-one logic defect; misrepresents state to every default consumer) | Either default `current_step` to `0`, or compare `index === this.current_step - 1`. |
| F-037 | (multiple — axe sweep) | MEDIUM | The axe-core sweep is not reproducible run-to-run in either theme, confirmed across 3 runs in this task's own session plus 5 earlier sessions across Tasks 3/4. Light: 14 instances/14 stories (this task's designated `raw-a11y.txt` run) vs. 16/16 (two immediate reruns, `raw-a11y-rerun{1,2}.txt`) — driven by two stories that flip in/out: `components-range-slider--default` and `components-stepper-horizontal-stepper--default`. Dark: 28 vs. 29 instances (distinct-story count stable at 25) — driven by `Components/Header Default` reading 0/1/2 violations across at least 6 sessions. Union distinct-story count (29) and dark distinct-story count (25) are the only figures that have never moved. | — (tooling reliability, not itself a WCAG failure) | Before Task 9 turns this sweep into a CI gate, decide and document the variance the gate tolerates — do not gate on a single run's exact instance count. |
| F-038 | (repo-level) | MEDIUM — **fixed on this branch** | `package-lock.json` pinned `@stencil/core` 4.24.0 while the working tree had drifted to 4.43.5, so `npm ci` produced a build different from what developers were testing against. | — | Fixed on this branch; both `packages/stencil/package.json` and `package-lock.json` now pin `4.43.5` exactly, confirmed by `npx stencil build` reporting `v4.43.5`. No further action. |
| F-039 | dda-banner, dda-ui-card | MEDIUM | Neither has a `.stories.tsx` file (confirmed by directory listing). Because the axe sweep, WCAG 2.5.8, and WCAG 2.1.1 checks only run against stories, these two components were never exercised by any of those three tools — their "0" in the baseline's per-component table means **not measured**, not measured-and-clean (see the component matrix, which marks both N/A rather than PASS on those columns). `dda-ui-card` additionally has a dead `linkClick` event (F-035) and `dda-banner` has undefined slider CSS and no interactivity (F-026) that should be fixed before a story is written, or the story will document broken behavior as if it were the design. | — (coverage gap) | Fix F-026/F-035 first, then write `.stories.tsx` and `-docs.mdx` for both. |
| F-040 | (multiple) | LOW | ESLint: 46 errors, 4 warnings across the codebase (per-component breakdown in `docs/a11y/baseline-2026-08-20.md`'s "Faults by component" table; largest concentration `dda-header` at 6/4). | — | See `eslint-report.json` / `raw-lint.txt` for line-level detail; not re-triaged line-by-line here. |
| F-041 | (multiple) | LOW | Prettier: 93 files fail `format:check`. | — | Run `npm run format` (or equivalent) from `packages/stencil`; see `raw-format.txt`. |
| F-042 | (multiple) | LOW | 105 ghost attributes (106 total minus the 1 informational case at I-001) remain across the codebase's `.stories.tsx` files, per the baseline's per-component counts (`dda-sticky-footer` 43, `dda-header` 18, `dda-search-input` 8, `dda-button` 6, and 16 more components with smaller counts — see the baseline's "Faults by component" table and this document's component matrix `API correct` column). Not individually triaged by file:line in this task beyond the specific cases already called out (F-029, F-030, and the component matrix's per-row footnotes). | — | Triage per-component against `raw-api.txt` / `eslint-report.json`; each is a `.stories.tsx` `args`/`argTypes` key with no matching `@Prop()`. |
| F-043 | (repo-level) | LOW | ESLint's `files: ['src/**/*.tsx']` scope excludes `.js` files entirely, so `src/utils/applyTheme.js` is unlinted except for whatever the base config still applies — including its 1 `no-undef` error on `document`, which has no home in the per-component table because it isn't a component file. | — | Extend the ESLint `files` glob (or add a second override) to cover `src/utils/*.js`. |
| F-044 | dda-header | LOW | Two `onClick`s on non-button elements are architecturally on the wrong element but not functionally broken: `dda-header.tsx:271`'s hamburger `div` only works for keyboard users because the click event bubbles up from a real nested `<button>` at `:273`; `dda-header.tsx:268`'s overlay `div` is not independently keyboard-operable but doesn't need to be — the same menu-close is reachable via a real `<button>` (`:340`) and `Escape` (`:82-89,95`). | — (code smell, not a defect) | Move the `onClick` at `:271` to the nested `<button>` directly, for correctness rather than incidental bubbling. |
| F-045 | dda-attach-file | LOW | Two `<label for="X">` elements can target the same input `id` when a consumer sets both `label` and leaves the default "Choose File" text — an implicit (nesting) and explicit (`for`/`id`) association both present. Not broken (browsers/screen readers concatenate or pick the first) but redundant. `dda-attach-file.tsx:76,91-93` | — | Consolidate to one label. |
| F-046 | (tooling) | LOW | The automated WCAG 2.1.1 checker (`packages/stencil/scripts/wcag22-checks.ts:3`) builds its "should be reachable" set from a fixed selector (`a[href], button, input, select, textarea, [tabindex]:not([tabindex="-1"])`). It can only verify that elements *already matching* that selector are Tab-reachable — a `<div>`/`<span>` with an `onClick` that was never given a `tabindex` is invisible to the check on both sides of the comparison, which is why F-001/F-002/F-003 (and the equivalent pattern in F-010) show as "0 failures" in the automated 2.1.1 total. Documented here as a known limitation for whoever next touches this checker. | — | Extend the checker to also flag elements carrying an `onClick`-equivalent handler but no interactive role/tabindex, or accept this class of defect must be caught by manual review going forward. |

---

## Open Items — genuinely unresolved, need a live browser/AT check

Not failures, not passes. Carried forward exactly as recorded in the manual review,
because deciding them from source alone would be a guess.

- **O-001 (highest-value open item).** `dda-number-field` (`Disabled`, `Error` stories) and
  `dda-phonefield` (`Error` story) — 3 of the original 15 needs-manual-contrast entries.
  Their actual `<input>` carries class `dda-field-group-input`, not the generic
  `dda-input-field` class the codebase's main focus ring (`global/input.css:76-79`)
  targets, so that rule never applies. The mechanism that *does* change on focus is
  `global/input.css:341-342`: `border-inline-start: solid 1px var(--dda-color-primary-40)`
  — a single 1px colored edge appears where none existed before. This is a real, if weak,
  focus indicator — not "no change at all." Whether a 1px partial-edge border reliably
  reads as "focused" at real-world screen density/zoom needs a live render; a static
  token comparison can't settle it. Recommend Task 9 treat the weak mechanism as a design
  smell regardless (a full-border or box-shadow ring removes the ambiguity) rather than
  wait on a contrast verdict a screenshot would resolve in seconds.
- **O-002.** `dda-checkbox`/`dda-radiobutton` — when both a `for`-linked `<label>`
  (visible `title_text`) and `aria-label` are set, `aria-label` typically wins as the
  accessible name in most browsers, potentially announcing text out of sync with what's
  displayed. `dda-checkbox.tsx:34-35,38`, `dda-radiobutton.tsx:34-35`. Structurally
  confirmed; not confirmed audible in a specific screen reader.
- **O-003.** `dda-breadcrumb.tsx:32` renders every crumb, including the current/last one,
  as a real `<a href={crumb.url}>`. Worth asking whether the current crumb should stay a
  clickable link to its own URL, or be de-linked / marked `aria-current="page"` (see
  F-027) — not run down here.
- **O-004.** `dda-pagination.tsx:111`'s page-number buttons render only the bare digit as
  their accessible name (no `aria-label`). Worth asking whether "3" read in isolation is
  sufficient, or whether each needs `aria-label="Page 3"`.
- **O-005.** `dda-credit-card.tsx:38` (`**** {this.card_number.slice(-4)}`) — not tested
  against real assistive technology output; unclear whether "**** 1234" reads sensibly
  aloud or needs an `aria-label` summary ("card ending in 1234").
- **O-006.** `dda-vertical-stepper`'s `current_Step` prop (`dda-vertical-stepper.tsx:10`,
  see F-034) — which HTML attribute name it compiles to (`current_step`, `currentstep`,
  or something else) was not verified without a build.
- **O-007.** `dda-header`'s WCAG 2.4.11 risk (F-009) — the absence of scroll-padding
  mitigation is a confirmed code fact; the exact pixel overlap on any specific page,
  which depends on the header's rendered height at that breakpoint versus how far the
  page's content sits from the top, needs a live scroll-and-tab test to quantify.

## Informational — confirmed non-defects, not queued for repair

- **I-001.** `dda-breadcrumb`'s `breadcrumbs` ghost attribute (1 of the 106 counted in
  F-042/baseline) is a known-benign false-positive-by-design:
  `dda-breadcrumb.stories.tsx` destructures `breadcrumbs` out of `args` and re-encodes it
  as a real `data-breadcrumbs` attribute (`const { breadcrumbs, ...otherArgs } = args`).
  The rendered markup works correctly. Counted in the 106 total because the checker's
  literal contract flags any `args` key without a matching `@Prop()` — not because it's a
  real defect. **Do not rank for repair.**
- **I-002.** `dda-button.css`'s `.light-mode.btn-color-*` rule set (e.g. `:30-45,63-75,
  91-101,333-336,...`) is reachable, live code — not dead code an earlier draft mistakenly
  flagged for removal. `component_mode="light-mode"` is a real, documented value
  (`dda-button.stories.tsx:58-62`; the same pattern recurs in ~28 components' stories). It
  fails with the exact same computed numbers as F-019/F-020 because it resolves to the
  same underlying color tokens in light theme — not a separate defect, already covered by
  F-019/F-020.
- **I-003.** `dda-phonefield` and `dda-creditcard-field` do not block paste (grepped both
  for `paste`/`onPaste`/`preventDefault` — no matches in either; both reformat on
  `onInput` without rejecting the action). Confirmed WCAG 3.3.8 compliant on this specific
  point. No action needed.
- **I-004.** `dda-range-slider` correctly implements native keyboard/ARIA slider
  semantics via two real `<input type="range">` elements (`dda-range-slider.tsx:64-76,
  78-92`) — implicit `role="slider"` and `aria-valuenow/min/max` come from the browser
  automatically, and arrow keys work with no custom code. The one exception to this
  codebase's pattern of custom, under-implemented widgets (`dda-select`, `dda-dropdown`,
  `dda-tabs`). Its separate WCAG 2.5.8 target-size failure (F-022) is about hit-area size,
  not this keyboard/ARIA question.

## Note on review depth

`dda-breadcrumb`, `dda-pagination`, and `dda-credit-card` received a lighter-touch check
than the other 31 components — they had no dedicated question in the manual review's
brief for their group. They are **not cleared**; they are less examined than everything
else in this report. Their entries above (F-025 for breadcrumb, F-027 for both, O-003,
O-004, F-033, O-005 for credit-card) are real but should not be read as an exhaustive
audit of those three.
