import { Meta, Markdown } from '@storybook/blocks';
import readme from './readme.md';
import { componentApi } from '../../storybook/component-api';

<Meta title="Components/Select/Documentation" />

# Select

The `dda-select` component renders a labelled trigger button that opens a list of options. Pass
the options as a JSON array string. When the user picks a different option, the component updates
its `selected` property and fires `selectionChange` with `detail.value`, the new option.

## API

<Markdown>{componentApi(readme)}</Markdown>

## Usage

### Vanilla JS

**From the CDN.** Replace `X.X.X` with the exact package version.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@dubai-design-system/components-js@X.X.X/dist/dda/dda.css" />
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
<script type="module">
  import { defineCustomElements } from 'https://cdn.jsdelivr.net/npm/@dubai-design-system/components-js@X.X.X/loader/index.es2017.js';
  defineCustomElements();
</script>

<dda-select
  button_id="emirate"
  label="Emirate"
  helper_text="Select the emirate of your residence"
  options='["Dubai","Abu Dhabi","Sharjah"]'
></dda-select>

<script>
  document
    .querySelector('dda-select')
    .addEventListener('selectionChange', event => console.log('Emirate:', event.detail.value));
</script>
```

**From npm with a bundler (Vite, Rollup).** Import the component file; it defines the element.

```js
import '@dubai-design-system/components-js/dist/dda/dda.css';
import '@dubai-design-system/components-js/dist/components/dda-select.js';

const select = document.querySelector('dda-select');
select.options = JSON.stringify(['Dubai', 'Abu Dhabi', 'Sharjah']);
select.addEventListener('selectionChange', event => {
  console.log('Emirate:', event.detail.value);
});
```

### React

```jsx
import { useState } from 'react';
import { DdaSelect } from '@dubai-design-system/components-react';

export function EmirateSelect() {
  const [emirate, setEmirate] = useState('');

  return (
    <DdaSelect
      button_id="emirate"
      label="Emirate"
      helper_text="Select the emirate of your residence"
      options={JSON.stringify(['Dubai', 'Abu Dhabi', 'Sharjah'])}
      selected={emirate}
      onSelectionChange={event => setEmirate(event.detail.value)}
    />
  );
}
```

### Vue

```html
<script setup>
import { ref } from 'vue';
import { DdaSelect } from '@dubai-design-system/components-vue';

const emirate = ref('');
const options = JSON.stringify(['Dubai', 'Abu Dhabi', 'Sharjah']);
</script>

<template>
  <DdaSelect
    button_id="emirate"
    label="Emirate"
    helper_text="Select the emirate of your residence"
    :options="options"
    :selected="emirate"
    @selectionChange="event => (emirate = event.detail.value)"
  />
</template>
```

### Angular

```html
<dda-select
  button_id="emirate"
  label="Emirate"
  helper_text="Select the emirate of your residence"
  options='["Dubai","Abu Dhabi","Sharjah"]'
  (selectionChange)="saveEmirate($event.detail.value)"
></dda-select>
```

## Errors

Set `error="error"` to show the error styling and `error_message` to show the message. The message
is linked to the trigger with `aria-describedby`, and the trigger gets `aria-invalid="true"`.

```html
<dda-select
  button_id="emirate"
  label="Emirate"
  options='["Dubai","Abu Dhabi","Sharjah"]'
  error="error"
  error_message="Select an emirate"
></dda-select>
```

## Keyboard

- **Arrow Down / Arrow Up** on the trigger opens the list and moves focus to the selected option.
- **Arrow Down / Arrow Up, Home, End** move between options.
- **Enter / Space** selects the focused option and returns focus to the trigger.
- **Escape** closes the list. **Tab** closes the list and moves focus on.

## Accessibility

- The component uses `button_id` for the trigger id and the label, list, helper text and error
  message ids. If you do not set `button_id`, the component generates a unique id. Set it when your
  own code or tests must find the trigger by id, and keep it unique.
- With a visible `label`, the trigger gets `aria-labelledby` that points at the label and at the
  trigger itself. Screen readers announce the label and the current value, for example
  `Emirate Dubai`. A click on the label opens the list.
- Without a visible `label`, set `aria_label`. When `aria_label` is set, it is the trigger name.
