# dda-accordion



<!-- Auto Generated Below -->


## Properties

| Property           | Attribute          | Description                                                                                            | Type                         | Default              |
| ------------------ | ------------------ | ------------------------------------------------------------------------------------------------------ | ---------------------------- | -------------------- |
| `accordion_icon`   | `accordion_icon`   | Material Symbols icon name shown before the header text, e.g. `info`.                                  | `string`                     | `'info'`             |
| `body_description` | `body_description` | Paragraph text shown in the panel when it is open. Slotted content follows it.                         | `string`                     | `''`                 |
| `component_mode`   | `component_mode`   | Theme override class for the container, e.g. `light-mode`.                                             | `string`                     | `undefined`          |
| `custom_class`     | `custom_class`     | Extra CSS classes added to the accordion container.                                                    | `string`                     | `''`                 |
| `design`           | `design`           | Container style: `bg-border` (filled background with border) or `no-border` (no background or border). | `"bg-border" \| "no-border"` | `'bg-border'`        |
| `header_text`      | `header_text`      | Text shown in the header button that opens and closes the panel.                                       | `string`                     | `'Accordion Header'` |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
