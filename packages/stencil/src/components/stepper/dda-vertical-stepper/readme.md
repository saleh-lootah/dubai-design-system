# dda-vertical-stepper



<!-- Auto Generated Below -->


## Properties

| Property         | Attribute        | Description                                                                                                                                 | Type     | Default     |
| ---------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------- | ----------- |
| `component_mode` | `component_mode` | Class added to the stepper container, e.g. `light-mode`.                                                                                    | `string` | `undefined` |
| `current_Step`   | `current_-step`  | <span style="color:red">**[DEPRECATED]**</span> Use `current_step`. Its attribute is `current_-step`, so it never worked in HTML.<br/><br/> | `number` | `0`         |
| `current_step`   | `current_step`   | Index of the current step, from 0. Steps up to and including it are active.                                                                 | `number` | `undefined` |
| `custom_class`   | `custom_class`   | Extra CSS classes added to the stepper container.                                                                                           | `string` | `''`        |
| `steps`          | `steps`          | Steps, as a JSON string array of `{ icon, title, subtitle, description }` objects. `icon` is a Material Icons name.                         | `string` | `undefined` |


----------------------------------------------

*Built with [StencilJS](https://stenciljs.com/)*
