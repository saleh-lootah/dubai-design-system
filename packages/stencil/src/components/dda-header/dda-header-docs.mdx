import { Meta, Markdown } from '@storybook/blocks';
import readme from './readme.md';
import { componentApi } from '../../storybook/component-api';

<Meta title="Components/Header/Documentation" />

# Header

The `dda-header` component renders the site header: two logos, the main navigation with mega
menus, a side menu, search, an accessibility panel (contrast, screen reader, text size), a
language button and a Login link. Pass the menus as JSON strings and listen to the events to
react to the user's choices.

## API

<Markdown>{componentApi(readme)}</Markdown>

## Usage

### Vanilla JS

**From the CDN.** Replace `X.X.X` with the exact package version.

```html
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/@dubai-design-system/components-js@X.X.X/dist/dda/dda.css" />
<link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons" />
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
<script type="module">
  import { defineCustomElements } from 'https://cdn.jsdelivr.net/npm/@dubai-design-system/components-js@X.X.X/loader/index.es2017.js';
  defineCustomElements();
</script>

<dda-header
  first-logo-src="/img/government-of-dubai.svg"
  first-logo-white-src="/img/government-of-dubai-white.svg"
  first-logo-alt="Government of Dubai"
  second-logo-src="/img/entity-logo.svg"
  second-logo-white-src="/img/entity-logo-white.svg"
  second-logo-alt="Dubai Digital Authority"
  login-link="/login"
  language_text="العربية"
  search_action="/search"
  quick-links='[{"label":"Home","href":"/"},{"label":"Services","menuLabel":"Services","href":"#","subMenu":[{"title":"Renew a trade licence","description":"Renew your licence online","icon":"badge","href":"/services/licence"}]}]'
  side-menu-items='[{"label":"Home","href":"/"},{"label":"Services","href":"#","subMenu":[{"headerLabel":"Services","label":"Business","href":"/services/business"}]},{"label":"Contact Us","href":"/contact"}]'
></dda-header>
```

**From npm with a bundler (Vite, Rollup).** Import the component file; it defines the element.

```js
import '@dubai-design-system/components-js/dist/dda/dda.css';
import '@dubai-design-system/components-js/dist/components/dda-header.js';

const header = document.querySelector('dda-header');
header.quickLinks = JSON.stringify([
  { label: 'Home', href: '/' },
  { label: 'About', href: '/about' },
]);
header.sideMenuItems = JSON.stringify([
  { label: 'Home', href: '/' },
  { label: 'Services', href: '#', subMenu: [{ headerLabel: 'Services', label: 'Business', href: '/services/business' }] },
]);

header.addEventListener('languageSwitch', () => console.log('switch language'));
header.addEventListener('lgTextSize', () => console.log('larger text'));
header.addEventListener('searchSubmit', event => console.log('search', event.detail.query));
```

### React

```jsx
import { DdaHeader } from '@dubai-design-system/components-react';

const quickLinks = [
  { label: 'Home', href: '/' },
  { label: 'About', href: '/about' },
];
const sideMenuItems = [
  { label: 'Home', href: '/' },
  { label: 'Contact Us', href: '/contact' },
];

export function SiteHeader() {
  return (
    <DdaHeader
      firstLogoSrc="/img/government-of-dubai.svg"
      firstLogoAlt="Government of Dubai"
      secondLogoSrc="/img/entity-logo.svg"
      secondLogoAlt="Dubai Digital Authority"
      loginLink="/login"
      language_text="العربية"
      quickLinks={JSON.stringify(quickLinks)}
      sideMenuItems={JSON.stringify(sideMenuItems)}
      onLanguageSwitch={() => console.log('switch language')}
      onSearchSubmit={event => console.log('search', event.detail.query)}
    />
  );
}
```

### Vue

```html
<script setup>
import { DdaHeader } from '@dubai-design-system/components-vue';

const quickLinks = JSON.stringify([
  { label: 'Home', href: '/' },
  { label: 'About', href: '/about' },
]);
const sideMenuItems = JSON.stringify([
  { label: 'Home', href: '/' },
  { label: 'Contact Us', href: '/contact' },
]);
</script>

<template>
  <DdaHeader
    firstLogoSrc="/img/government-of-dubai.svg"
    firstLogoAlt="Government of Dubai"
    secondLogoSrc="/img/entity-logo.svg"
    secondLogoAlt="Dubai Digital Authority"
    loginLink="/login"
    language_text="العربية"
    :quickLinks="quickLinks"
    :sideMenuItems="sideMenuItems"
    @languageSwitch="console.log('switch language')"
    @searchSubmit="event => console.log('search', event.detail.query)"
  />
</template>
```

### Angular

```html
<dda-header
  first-logo-src="/img/government-of-dubai.svg"
  first-logo-alt="Government of Dubai"
  second-logo-src="/img/entity-logo.svg"
  second-logo-alt="Dubai Digital Authority"
  login-link="/login"
  language_text="العربية"
  [quickLinks]="quickLinksJson"
  [sideMenuItems]="sideMenuItemsJson"
  (languageSwitch)="switchLanguage()"
  (searchSubmit)="search($event.detail.query)"
></dda-header>
```

In the component class, set `quickLinksJson` and `sideMenuItemsJson` to `JSON.stringify(...)` of
the menu arrays.

## Menus

`quick-links` builds the main navigation. An item without `subMenu` is a plain link. An item with
a `subMenu` opens a mega menu titled `menuLabel`; each entry shows a Material Symbols `icon`, a
`title` and a `description`.

`side-menu-items` builds the side menu that the hamburger button opens. A `subMenu` opens a nested
list; `headerLabel` of its first item is the list title. Sub-menus can nest to any depth.

## Menu shapes

`quick-links` accepts two shapes for its items. The header tells them apart by their fields, so
one array cannot mix both shapes.

**5.x shape:** `{ label, href, menuLabel, subMenu }`. An item with `subMenu` opens a mega menu
titled `menuLabel`; each `subMenu` entry is `{ title, description, icon, href }` and shows no icon
when `icon` is not set.

```json
[{ "label": "Services", "menuLabel": "Services", "href": "#", "subMenu": [
  { "title": "Renew a trade licence", "description": "Renew your licence online", "icon": "badge", "href": "/services/licence" }
] }]
```

**3.x shape:** `{ type, headerMenuLabel, url, children }`. A `dda_default_submenu` item with
`children` opens a dropdown list; a nested `children` array on one of those items opens a
second-level fly-out. A `dda_main_megamenu` item opens a mega menu instead, with each column as a
`children` entry and its links in `items`. A mega-menu item without `quickLinksIcon` shows
`sentiment_satisfied`, matching 3.x; the 5.x shape has no such fallback.

```json
[{ "type": "dda_default_submenu", "headerMenuLabel": "About us", "url": "#", "children": [
  { "headerMenuLabel": "Our strategy", "url": "#" },
  { "headerMenuLabel": "Policies", "url": "#", "children": [
    { "headerMenuLabel": "Quality policy", "url": "#" }
  ] }
] }]
```

## Accessibility panel

The panel has no built-in effect on the page. It emits events, and your code applies the change:

<table>
  <thead><tr><th>Control</th><th>Event</th></tr></thead>
  <tbody>
    <tr><td>Normal, Colours Blind, Red Weakness, Green Weakness</td><td><code>normalContrast</code>, <code>blindContrast</code>, <code>redContrast</code>, <code>greenContrast</code></td></tr>
    <tr><td><code>A</code>, <code>A+</code>, <code>A-</code></td><td><code>baseTextSize</code>, <code>lgTextSize</code>, <code>smTextSize</code></td></tr>
    <tr><td>Listen</td><td>Opens <code>read-speaker-link</code></td></tr>
  </tbody>
</table>

The Screen Reader item shows only when `read-speaker-link` is set.

## Links and labels

- **Logo links.** `first-logo-href` and `second-logo-href` set the link of each logo. Both default
  to `/`, the root of the domain. If your site is served under a subpath (for example
  `https://example.gov.ae/services/`), set them to your home page, such as `/services/` or `./`.
- **Menu button.** `menu_button_label` is the accessible name of the hamburger button. Default:
  `Menu`.
- **Language buttons.** `language_text` is the label of both language buttons (default `العربية`).
  `language_lang` sets their `lang` attribute (default `ar`), so screen readers read the label in
  that language. When the label is English, set `language_lang="en"`.
- **Login link.** `login-text` and `login-icon` set the label and the Material Symbols icon of the
  Login link in the desktop toolbar and the side menu (defaults `Login` and
  `sentiment_satisfied`). `login-link` sets its URL. To remove the link from both places, set
  `hide_login`. An empty `login-text` does not hide the link; it shows the default label.

## Text and language

Most of the text in the accessibility panel, the tooltips, the search placeholder and the
side-menu titles takes an attribute, so a page can show its own language. (A few close-button
labels do not; see the note after the table.)

- An absent attribute shows the English default.
- An empty attribute (`""`) removes that text.
- **Tooltips are the exception.** A tooltip must have text, so an empty tooltip attribute falls
  back to its default: `accessibility_tooltip`, `search_tooltip`, `language_tooltip`,
  `login_tooltip` and `menu_button_label` (the hamburger button's tooltip) all keep their default
  text when set to `""`. `menu_button_label` also names the hamburger button visually; an empty
  value clears the visible label but the tooltip still shows `Menu`.
- `login-text=""` and `language_text=""` remove the Login link and the language button, as 3.x did.

<table>
  <thead><tr><th>Prop</th><th>Default</th><th>Where it shows</th></tr></thead>
  <tbody>
    <tr><td><code>contrast_title</code></td><td>Contrast</td><td>Accessibility panel, contrast column heading</td></tr>
    <tr><td><code>contrast_description</code></td><td>Select your preferred contrast setting</td><td>Accessibility panel, contrast column</td></tr>
    <tr><td><code>contrast_normal_text</code> (3.x: <code>contrast_noraml_text</code>)</td><td>Normal</td><td>Accessibility panel, Normal option</td></tr>
    <tr><td><code>contrast_color_blind_text</code></td><td>Colours Blind</td><td>Accessibility panel, Colours Blind option</td></tr>
    <tr><td><code>contrast_red_weakness_text</code></td><td>Red Weakness</td><td>Accessibility panel, Red Weakness option</td></tr>
    <tr><td><code>contrast_green_weakness_text</code></td><td>Green Weakness</td><td>Accessibility panel, Green Weakness option</td></tr>
    <tr><td><code>screen_reader_title</code></td><td>Screen Reader</td><td>Accessibility panel, screen reader column heading</td></tr>
    <tr><td><code>screen_reader_description</code></td><td>Listen to the content of the page by clicking play or listen</td><td>Accessibility panel, screen reader column</td></tr>
    <tr><td><code>screen_reader_link_label</code></td><td>Listen to this page using ReadSpeaker</td><td>ReadSpeaker play link, accessible name</td></tr>
    <tr><td><code>text_size_title</code></td><td>Text Size</td><td>Accessibility panel, text size column heading</td></tr>
    <tr><td><code>text_size_description</code></td><td>Use the buttons below to increase or decrease the text size</td><td>Accessibility panel, text size column</td></tr>
    <tr><td><code>accessibility_tooltip</code></td><td>Accessibility</td><td>Desktop accessibility button, tooltip</td></tr>
    <tr><td><code>accessibility_button_text</code></td><td>Accessibility</td><td>Accessibility buttons, visually hidden name (desktop and side menu)</td></tr>
    <tr><td><code>search_tooltip</code></td><td>Search</td><td>Desktop search, tooltip</td></tr>
    <tr><td><code>search_input_placeholder</code></td><td>Search (or <code>searchText</code>, if set)</td><td>Search inputs, placeholder (desktop and mobile)</td></tr>
    <tr><td><code>language_tooltip</code></td><td>Language</td><td>Desktop language button, tooltip</td></tr>
    <tr><td><code>login_tooltip</code></td><td>The Login label</td><td>Desktop Login link, tooltip</td></tr>
    <tr><td><code>menu_button_label</code></td><td>Menu</td><td>Hamburger button, visible label and tooltip</td></tr>
    <tr><td><code>side-main-menu-title</code></td><td>Quick Links</td><td>Side menu, heading above the main list</td></tr>
    <tr><td><code>side-other-menu-title</code></td><td>(none)</td><td>Side menu, heading above the "other" list; shows only when there are other links</td></tr>
  </tbody>
</table>

Not every visible text is a prop. Five English labels have no attribute: the mega-menu, side-menu
and desktop-accessibility close buttons all read "Close Sidebar", the mobile accessibility panel's
close button reads "Close Accessibility", and the mobile search panel's close button reads "Close
search".

## Search

The header search is a `<form role="search">`. Enter submits it on desktop. On small screens the
search button opens a search field under the header; Escape or the close button dismisses it.
Empty searches do nothing.

Every non-empty search emits a cancelable `searchSubmit` event with `detail.query`. What
happens next depends on two things:

<table>
  <thead><tr><th><code>search_action</code> set</th><th>Event cancelled</th><th>Result</th></tr></thead>
  <tbody>
    <tr><td>yes</td><td>no</td><td>The browser goes to <code>search_action?q=&lt;query&gt;</code> (a plain GET, no JavaScript needed).</td></tr>
    <tr><td>yes</td><td>yes</td><td>No navigation; your code handles the search.</td></tr>
    <tr><td>no</td><td>either</td><td>No navigation; your code handles the search.</td></tr>
  </tbody>
</table>

The query parameter is named by `search_input_name` and defaults to `q`.

#### Static site: send searches to a results page

```html
<dda-header search_action="/search.html" search_input_name="q"></dda-header>
```

`/search.html` reads the query with `new URLSearchParams(location.search).get('q')`.

#### Framework app: route the search yourself

```jsx
// React
<DdaHeader
  search_action="/search"
  onSearchSubmit={event => {
    event.preventDefault();
    navigate(`/search?q=${encodeURIComponent(event.detail.query)}`);
  }}
/>
```

Keeping `search_action` set means the search still works if JavaScript has not loaded yet.
